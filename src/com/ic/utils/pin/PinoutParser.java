package com.ic.utils.pin;

import java.io.*;

public class PinoutParser {
    BufferedReader reader;
    public PinoutParser(InputStream inputStream)    {
        this.reader = new BufferedReader(new InputStreamReader(inputStream));
    }
    public void generatePinout()    {
        while(true) {

        }
    }
}
